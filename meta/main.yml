---
galaxy_info:
  author: MongoDB
  description: Disabled the apt-daily services so that packages can be installed on boot
  license: Apache
  min_ansible_version: 2.8
  platforms:
    - name: Ubuntu
      versions:
        - xenial
        - bionic
  galaxy_tags:
    - disable-apt-daily
